import { EventEmitter } from '../../../../stencil-public-runtime';
import { type FolioFilters, type FolioRow, type FolioSummary } from '../types';
import type { ICurrency } from "../../../../models/property";
export declare class IrCityLedgerFolioTable {
    agentId: number | null;
    propertyId: number;
    currencySymbol: string;
    currencies: ICurrency[];
    filters: FolioFilters;
    private tableState;
    private data;
    private isLoading;
    private startingBalance;
    private selectedRowIds;
    private holdTargetRow;
    private totalCount;
    private pageIndex;
    private pageSize;
    private closingBalance;
    folioSummaryLoaded: EventEmitter<FolioSummary>;
    generateInvoice: EventEmitter<FolioRow[]>;
    private columnHelper;
    private pageSizes;
    private cityLedgerService;
    private holdDialogRef;
    handleAgentIdChange(newValue: number | null, oldValue: number | null): Promise<void>;
    handleFiltersChange(newFilters: FolioFilters): Promise<void>;
    /** Single source of truth for date formatting throughout this component. */
    private formatDate;
    private clearData;
    private sortFolioRows;
    private fetchFolioData;
    private get selectedUnbilledRows();
    private handleHoldToggled;
    private getSymbol;
    private columns;
    private onTableStateChange;
    private renderCell;
    private renderTableHead;
    private renderStartingBalanceRow;
    private renderEndingBalanceRow;
    private renderDataRows;
    render(): any;
}
