export declare class RequirementCheck {
    /**
     * Whether this requirement has been satisfied (true/false).
     */
    isValid: boolean;
    /**
     * The requirement text to display (e.g. "At least one lowercase letter").
     */
    text: string;
    render(): any;
}
