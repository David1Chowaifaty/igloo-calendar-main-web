import { c as createStore } from './index-Cn49IR5D.js';

const initialState = {
    entries: null,
    direction: 'ltr',
};
const { state: locales} = createStore(initialState);

export { locales as l };
