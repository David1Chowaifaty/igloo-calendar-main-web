import { a as createStore } from './index-C63jMJYk.js';

const initialState = {
    entries: null,
    direction: 'ltr',
    language: 'en',
};
const { state: locales} = createStore(initialState);

export { locales as l };
