import { c as createStore } from './index-BX-r5OtJ.js';

const initialState = {
    entries: null,
    direction: 'ltr',
};
const { state: locales} = createStore(initialState);

export { locales as l };
