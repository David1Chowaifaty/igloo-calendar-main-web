import { c as createStore } from './index-CR94o8Bs.js';

const initialState = {
    entries: null,
    direction: 'ltr',
};
const { state: locales} = createStore(initialState);

export { locales as l };
