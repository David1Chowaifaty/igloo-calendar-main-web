import { createStore } from "@stencil/store";
const initialState = {
    countries: [],
};
export const { state: phone_input_store } = createStore(initialState);
export default phone_input_store;
//# sourceMappingURL=phone.store.js.map
